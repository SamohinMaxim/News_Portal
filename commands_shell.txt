1. Создать двух пользователей (с помощью метода User.objects.create_user('username')).
user1 = User.objects.create_user('username1')
user2 = User.objects.create_user('username2')

2. Создать два объекта модели Author, связанные с пользователями.
author1 = Author.objects.create(user=user1)
author2 = Author.objects.create(user=user2)

3. Добавить 4 категории в модель Category.
Category.objects.create(a_category = 'sport')
Category.objects.create(a_category = 'politics')
Category.objects.create(a_category = 'education')
Category.objects.create(a_category = 'culture')

4. Добавить 2 статьи и 1 новость.
a1 = Post.object.create(author_post=a1, type_post='SP', title_post='Лыжня России – 2026» – скоро!', text_post='')
a2 = Post.objects.create(author_post=author1, type_post='PO', title_post='Дацин – Тюмень: к юбилею побратимских отношений', text_post='')

news1 = Post.objects.create(author_post=author1, type_post='NE', title_post='В Тюмени прошла рабочая встреча с профессиональным
туристическим сообществом',
text_post='Как рассказать о современной жизни города, не забывая о его прошлом? Какие новые подходы работы внедрить
в сфере туризма? Что нельзя упускать, рассказывая о Тюмени гостям? Эти и другие вопросы обсудили участники рабочей
встречи с тюменскими экскурсоводами.')

5. Присвоить им категории (как минимум в одной статье/новости должно быть не меньше 2 категорий).
a1.category_post.add(category4)
a1.category_post.add(category3)
a2.category_post.add(category1)
news1.category_post.add(category1)
news1.category_post.add(category3)
news1.category_post.add(category4)

6. Создать как минимум 4 комментария к разным объектам модели Post (в каждом объекте должен быть как минимум один комментарий).
Comment.objects.create(comment_post=Post.objects.get(id=1),comment_user=Author.objects.get(id=1).author,
feedback_text = 'Очень интересная статья')

Comment.objects.create(comment_post=Post.objects.get(id=2),comment_user = Author.objects.get(id=1).author,
feedback_text = 'Это интересно')

Comment.objects.create(comment_post = Post.objects.get(id=3),comment_user = Author.objects.get(id=2).author,
feedback_text = "Это потрясающе")

Comment.objects.create(comment_post=Post.objects.get(id=1),comment_user = Author.objects.get(id=2).author,
feedback_text = 'Очень хорошо')

7. Применяя функции like() и dislike() к статьям/новостям и комментариям, скорректировать рейтинги этих объектов.
Post.objects.get(id=1).like()
Post.objects.get(id=1).dislike()
Post.objects.get(id=2).like()
Post.objects.get(id=2).dislike()

Comment.objects.get(id=1).like()
Comment.objects.get(id=1).dislike()
Comment.objects.get(id=2).like()
Comment.objects.get(id=2).dislike()
Comment.objects.get(id=3).like()
Comment.objects.get(id=3).dislike()

Comment.objects.get(id=2).rating

8. Обновить рейтинги пользователей.
a1 = Author.objects.get(id=1)
a1.update_rating()

9. Вывести username и рейтинг лучшего пользователя (применяя сортировку и возвращая поля первого объекта).
a1.ratingAuthor
a1.post_set.all().values('rating')
a1.authorUser.comment_set.all().values('rating')

a2 = Author.objects.all().order_by('-ratingAuthor')[:1]
	a2
	for b in a2:
		b.authorUser.username
		b.ratingAuthor

10. Вывести дату добавления, username автора, рейтинг, заголовок и превью лучшей статьи, основываясь на лайках/дислайках к этой статье.
a3 = Post.objects.all().order_by('-rating')[:1]
for j in a3:
	f"Data : {j.dataCreations.day}"
	f"Rating: {j.rating}"
	j.author.authorUser
	j.title
	j.preview()

11. Вывести все комментарии (дата, пользователь, рейтинг, текст) к этой статье.
a4 = Comment.objects.all().filter(commentPost=a3)
for d in a4:
	F"Data : {d.dataCreation.day}/{d.dataCreation.month}/{d.dataCreation.year}"
	d.userPost
	F"Rating : {d.rating}"
	d.text